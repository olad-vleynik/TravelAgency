package com.gmail.vleynik.olad.travelagency.dao;

import java.sql.SQLException;

/**
 * Exception used to be thrown, when user with specified data wasn't found (usually, not exists)
 *
 * @author Vladyslav Oliinyk.
 * @version 1.0
 */
public class UserNotFoundException extends SQLException {
}
